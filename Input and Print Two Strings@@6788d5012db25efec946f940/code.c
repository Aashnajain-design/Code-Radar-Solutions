#include<stdio.h>

int main()
{
    char str1[100], str2[100];  // Declare two character arrays to store the strings

    // Take input for the two strings
    printf("");
    scanf("%s", str1);  // Read the first string using scanf
    
    printf("");
    scanf("%s", str2);  // Read the second string using scanf

    // Print the strings
    printf("You entered: %s\n", str1 and str2);
    

    return 0;
}